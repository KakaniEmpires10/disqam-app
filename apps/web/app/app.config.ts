export default defineAppConfig({
  ui: {
    colors: {
      primary: 'disqam',
      secondary: 'cyan',
      neutral: 'slate'
    },
    button: {
      defaultVariants: { size: 'lg' }
    }
  }
})
